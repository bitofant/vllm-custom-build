# Transient build artifacts (recreated by build-async.sh each run)
build.log
build.pid
build.exit
pr-description.md

# Local benchmark history (bench-inference.ts --report reads this)
bench-results/

# Python cache
__pycache__/
*.pyc

# Local, per-machine Claude settings (personal; not shared)
.claude/settings.local.json
